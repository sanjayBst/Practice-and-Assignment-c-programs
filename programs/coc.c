// calculate the circumference of circle

#include <stdio.h>
void main()
{
    float r, circum;
    float pie = 3.14;
    printf("Enter the radius of circle = ");
    scanf("%f", &r);
    circum = pie * r * r;
    printf("the cirumference of circle is %f", circum);
    getc;
}