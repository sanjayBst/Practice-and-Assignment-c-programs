// TO find total salary of an employee

#include <stdio.h>
void main()
{
    float ms,as;
    printf("enter your monthly salary = ");
    scanf("%f",&ms);
    as = 12*ms;
    printf("Total salary is %f",as);
    getc;
}